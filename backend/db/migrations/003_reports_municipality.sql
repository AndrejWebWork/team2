-- ==========================================================================
-- Миграција 003 — општина за пријавите (за админ: точна локација + општина)
-- Изврши еднаш врз постоечка база:
--   psql "$DATABASE_URL" -f backend/db/migrations/003_reports_municipality.sql
-- ==========================================================================

ALTER TABLE reports ADD COLUMN IF NOT EXISTS municipality TEXT;
